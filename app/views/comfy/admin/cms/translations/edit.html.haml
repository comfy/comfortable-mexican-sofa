.page-header
  = link_to t('.revision', count: @translation.revisions.count), comfy_admin_cms_site_page_translation_revisions_path(@site, @page, @translation), class: "btn btn-info pull-right"
  %h2= t('.title')

= comfy_form_for @translation, as: :translation, url: {action: :update} do |form|
  = render form
