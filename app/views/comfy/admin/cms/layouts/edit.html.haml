.page-header
  = link_to t('.revision', :count => @layout.revisions.count), comfy_admin_cms_site_layout_revisions_path(@site, @layout), :class => 'btn btn-default pull-right'
  %h2= t('.title')

- content_for :right_column do
  = render 'comfy/admin/cms/sites/mirrors', :object => @layout

= comfy_form_for @layout, :as => :layout, :url => {:action => :update} do |form|
  = render form
