.files
  - fragment_attachments.each do |attachment|
    - if attachment.image?
      - variant = attachment.variant(Comfy::Cms::File::VARIANT_SIZE[:thumb])
      - thumb = image_tag variant, size: "200x150"
    .file{id: dom_id(attachment), data: {toggle: "page-file-popover", content: thumb}}
      = link_to attachment.filename, attachment
