.page-header
  = link_to t('.revision', count: @snippet.revisions.count), comfy_admin_cms_site_snippet_revisions_path(@site, @snippet), class: "btn btn-info pull-right"
  %h2= t('.title')

= comfy_form_for @snippet, as: :snippet, url: {action: :update} do |form|
  = render form
